{
  "name": "cozynest-site",
  "version": "1.0.0",
  "description": "Cozynest — root package for Netlify deployment",
  "dependencies": {
    "serverless-http": "^3.2.0"
  },
  "devDependencies": {
    "wrangler": "^4.81.0"
  },
  "scripts": {
    "deploy": "wrangler deploy",
    "preview": "wrangler dev"
  }
}
